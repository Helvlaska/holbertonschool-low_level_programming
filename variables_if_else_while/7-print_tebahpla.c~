#include <stdio.h>
/**
 * main - print revert alphabet
 *
 * Return: 0 it's ok
 */

int main(void)
{
	int i;
	char a = 'a';
	char z = 'z';

	for (i = a; i >= z; ++i)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
