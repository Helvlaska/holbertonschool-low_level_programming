#include <stdio.h>
/**
 * main - print numbers of 0 to 9 with space and ,
 *
 * Return: 0 it's ok
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchat(i);
		putchar(44);
		putchar(32);
	}
	putchar('\n');
	return (0);
}
