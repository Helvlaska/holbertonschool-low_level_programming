#include <stdio.h>
/**
 * main - print with putchar numbers of 0 to 9
 *
 * Return: 0 it's ok
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
