#include "main.h"
#include <stdio.h>

/**
 * print_rev - prints a string, in reverse
 * @s: it's string value
 *
 */

void print_rev(char *s)
{
	char *end = s;

	while (*s != '\0')
	{
		end++;
	}
	end--;

	while (end >= s)
	{
		putchar(*end);
		end--;
	}
	_putchar('\n');
}
