#include "main.h"
/**
 * add - adds two integers and returns the result
 * @number1: it's an random number
 * @number2: it's an random number
 *
 * Return: 0 it's ok
 */

int add(int number1, int number2)
{
	int somme = (number1 + number2);

		return (somme);
}
